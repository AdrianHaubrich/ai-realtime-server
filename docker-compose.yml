services:
  ai-realtime-server:
    build: .
    image: ai-realtime-server:local
    container_name: ai-realtime-server
    env_file:
      - .env
    ports:
      - "3001:3001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=3001
    restart: unless-stopped
